# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Dave Robins

cmake_minimum_required(VERSION 3.24)
project(vspyshark VERSION 0.2.0 LANGUAGES C CXX)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_POSITION_INDEPENDENT_CODE ON)
if(MSVC)
    set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
    add_compile_options(/utf-8 /Zc:__cplusplus)
endif()
option(VSPYSHARK_BUILD_APP "Build the standalone application" ${PROJECT_IS_TOP_LEVEL})
option(VSPYSHARK_BUILD_TESTS "Build analyzer and UI tests" ${PROJECT_IS_TOP_LEVEL})
option(VSPYSHARK_BUILD_FWGUI_PLUGIN "Build the FreeWili GUI plugin" OFF)
option(VSPYSHARK_TEST_ENGINE "Match GUI hosts with ImGui test engine enabled" ON)
set(VSPYSHARK_WIRESPY_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../wirespy" CACHE PATH "Independent wirespy checkout")
set(VSPYSHARK_IMGUI_TARGET "" CACHE STRING "Existing host ImGui target")
include(cmake/Dependencies.cmake)
include(cmake/Capture.cmake)

add_library(vspyshark_gui STATIC src/shark_view.cpp src/services.cpp src/portable_ui.cpp)
target_include_directories(vspyshark_gui PUBLIC src)
target_link_libraries(vspyshark_gui PUBLIC vspyshark_core ${VSPYSHARK_IMGUI_TARGET})

include(cmake/Decoder.cmake)
if(VSPYSHARK_BUILD_APP)
    include(cmake/Application.cmake)
endif()
if(VSPYSHARK_BUILD_FWGUI_PLUGIN)
    add_subdirectory(plugins/freewiligui)
endif()
